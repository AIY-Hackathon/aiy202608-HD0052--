# === Part A: 后端核心依赖 ===
fastapi>=0.141
uvicorn[standard]>=0.52
sqlalchemy>=2.0
asyncpg>=0.31
aiosqlite>=0.22            # SQLite 异步驱动（本地开发默认）
redis>=8.1
pandas>=3.0
numpy>=2.5
python-multipart>=0.0.32
pydantic>=2.13
alembic>=1.18
pytest>=9.1
httpx>=0.28

# === Part C: 引擎/报告/AI 依赖（由开发者 C 添加）===
# reportlab>=4.0          # PDF 报告生成
# jinja2>=3.1             # HTML 报告模板
# gradio>=5.0             # AI 判读演示
# anthropic>=0.40         # Claude API

# === 注: pysam 在 Windows 无预编译包, 使用 pandas 方案 ===
